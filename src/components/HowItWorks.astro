---
const steps = [
  {
    n: '01',
    title: 'Elige tu plan',
    desc: 'Selecciona 1, 3, 6 o 12 meses en USD y escríbenos por WhatsApp.',
    color: 'yellow',
  },
  {
    n: '02',
    title: 'Coordinamos el pago',
    desc: 'Te confirmamos el método disponible y validamos tu compra sin enredos.',
    color: 'blue',
  },
  {
    n: '03',
    title: 'Instalas y quedas listo',
    desc: 'Te guiamos para instalar en Android o TV Box y empezar a ver en minutos.',
    color: 'red',
  },
];
---

<section id="como-funciona" class="relative py-28 bg-gradient-to-b from-black via-gray-950 to-black overflow-x-hidden">
  <div class="absolute inset-0 opacity-10">
    <div class="absolute top-10 left-10 w-72 h-72 bg-yellow-500 rounded-full blur-3xl"></div>
    <div class="absolute bottom-10 right-10 w-72 h-72 bg-blue-500 rounded-full blur-3xl"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <div class="mb-20 text-center">
      <div class="inline-block mb-6 px-8 py-3 bg-gradient-to-r from-yellow-500/10 via-blue-500/10 to-red-500/10 border-y-2 border-yellow-500/30">
        <span class="text-yellow-400 font-black text-sm tracking-widest uppercase">Cómo funciona</span>
      </div>

      <h2 class="text-5xl sm:text-6xl md:text-8xl font-black text-white mb-6 leading-[0.95]">
        ENTRAS
        <span class="block text-transparent bg-clip-text bg-gradient-to-r from-yellow-500 via-blue-500 to-red-500">SIN COMPLICARTE</span>
      </h2>

      <p class="text-xl md:text-2xl text-white/70 max-w-3xl mx-auto">
        Proceso simple, soporte real y activación asegurada.
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-8">
      {steps.map((s, index) => {
        const colorClass =
          s.color === 'yellow'
            ? 'border-yellow-500/30 hover:border-yellow-500'
            : s.color === 'blue'
              ? 'border-blue-500/30 hover:border-blue-500'
              : 'border-red-500/30 hover:border-red-500';

        const badgeClass =
          s.color === 'yellow'
            ? 'bg-yellow-500 text-black'
            : s.color === 'blue'
              ? 'bg-blue-500 text-black'
              : 'bg-red-500 text-black';

        return (
          <div class="group relative" style={`animation-delay:${index * 0.1}s;`}>
            <div class={`relative bg-gray-900/50 backdrop-blur border-2 ${colorClass} p-10 transition-all duration-500 group-hover:scale-[1.02]`}>
              <div class={`inline-flex items-center justify-center w-14 h-14 ${badgeClass} font-black text-lg mb-6`}>
                {s.n}
              </div>
              <h3 class="text-3xl font-black text-white mb-4">{s.title}</h3>
              <p class="text-white/70 text-lg leading-relaxed">{s.desc}</p>
              <div class="absolute -bottom-2 -right-2 w-10 h-10 border-b-4 border-r-4 border-white/10 group-hover:border-white/20 transition-colors"></div>
            </div>
          </div>
        );
      })}
    </div>

    <div class="mt-16 text-center">
      <a
        href="#planes"
        class="inline-flex items-center gap-3 px-12 py-6 bg-yellow-500 text-black font-black text-xl hover:bg-yellow-400 transition-all duration-300"
      >
        <span>VER PLANES</span>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M19 9l-7 7-7-7" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  [style*="animation-delay"] {
    animation: fadeInUp 0.7s ease-out backwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
  }
</style>
